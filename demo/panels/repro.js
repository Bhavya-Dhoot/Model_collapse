(function (root) {
  'use strict';
  function render(el, D) {
    var C = root.AT.chart;
    el.innerHTML = '<div class="grid-2"><div class="card"><h3>The experiment grid</h3><div class="stat-row"><div class="stat"><div class="v">' + D.meta.rows_total + '</div><div class="k">result rows in results/main.csv</div></div><div class="stat"><div class="v">' + D.meta.alphas.length + '</div><div class="k">anchoring fractions swept</div></div><div class="stat"><div class="v">' + D.meta.ns.length + '</div><div class="k">training budgets (n)</div></div><div class="stat"><div class="v">' + D.meta.terminal_generation + '</div><div class="k">generations per run</div></div></div><p class="sub">Three OpenML benchmarks (Adult, credit-g, bank-marketing) paired with three synthesizer families (Gaussian copula, TVAE-lite, CTGAN-lite), under two anchoring regimes (fixed and fresh). All scored every generation against a stratified holdout never trained on.</p><p class="sub" style="font-size: 12.5px; color: var(--ink-3); margin: 0;"><span class="mono">α: ' + D.meta.alphas.join(', ') + '</span> · <span class="mono">n: ' + D.meta.ns.join(', ') + '</span></p></div><div class="card"><h3>Reproducing this page</h3><ol style="margin: 0; padding-left: 18px; color: var(--ink-2);"><li style="margin-bottom: 8px;"><div class="eq">python code/run_experiment.py --out results/main.csv</div></li><li style="margin-bottom: 8px;"><div class="eq">python code/export_dashboard_data.py</div></li><li><div class="eq">python code/build_demo.py</div></li></ol><p class="sub">The first re-runs the full sweep; the second recomputes demo/data.json using the estimators from the paper\'s figure scripts; the third inlines that data into a self-contained demo/dashboard.html that opens with no server and no network.</p></div></div><div class="note">Every figure on this page is recomputed from the released results at page load — nothing is a stored image. The panels pinned to the paper\'s three-seed primary grid use ' + D.meta.primary_seeds + ' seeds; the fresh-versus-fixed and n-sweep comparisons use all ' + D.meta.seeds_max + ' seeds, exactly as each section of the paper states.</div>';
  }
  root.AT = root.AT || {}; root.AT.panels = root.AT.panels || {}; root.AT.panels.repro = { render: render };
}(window));
